function singUp() {
	var emailValue=document.getElementById("email").value;
	var passValue=document.getElementById("password").value;
	
	if(emailValue && passValue){
		//When both fields are filled
		
		window.location="ToDoodleListPage.html";
	}
}

function login(){
	var emailValue=document.getElementById("email").value;
	var passValue=document.getElementById("password").value;
	
	if(emailValue && passValue){
		//When both fields are filled
		
		window.location="ToDoodleListPage.html";
	}
}